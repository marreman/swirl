{
  "name": "swirl",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build-for-browser": "browserify src/swirl.js -o build/swirl.js -s swirl"
  },
  "devDependencies": {
    "browserify": "^14.5.0",
    "jest": "^21.2.1"
  }
}
